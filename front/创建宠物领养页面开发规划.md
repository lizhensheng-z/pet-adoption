# 创建宠物领养页面开发规划

> **创建时间**: 2026-02-15
> **页面路径**: `/src/views/org/PetCreate.vue`
> **路由**: `/org/pets/create`
> **目标用户**: 机构管理员

---

## 📋 目录

1. [页面功能概述](#页面功能概述)
2. [数据库模型分析](#数据库模型分析)
3. [接口需求分析](#接口需求分析)
4. [页面功能设计](#页面功能设计)
5. [接口详细设计](#接口详细设计)
6. [开发任务分解](#开发任务分解)
7. [技术实现要点](#技术实现要点)

---

## 1. 页面功能概述

### 1.1 页面定位
创建宠物页面是机构端的核心功能，用于机构管理员创建和管理待领养宠物的档案信息。这是一个多步骤表单页面，支持宠物基本信息、健康状况、性格特征、领养要求等完整信息的录入。

### 1.2 核心功能模块

| 模块 | 功能描述 | 权限要求 |
|------|---------|---------|
| **基本信息** | 宠物名称、物种、品种、年龄、性别、体型、毛色等 | `pet:create` |
| **健康状况** | 疫苗、绝育、驱虫状态及健康描述 | `pet:create` |
| **性格特征** | 性格描述、特征标签选择 | `pet:create` |
| **领养要求** | 领养条件和要求说明 | `pet:create` |
| **图片/视频上传** | 上传宠物照片（3-9张）和视频 | `pet:create` |
| **地理位置** | 显示/编辑宠物所在位置（默认继承机构地址） | `pet:create` |
| **保存草稿** | 保存为草稿状态，可继续编辑 | `pet:create` |
| **提交审核** | 提交到管理员审核 | `pet:submit_audit` |
| **预览** | 预览宠物信息展示效果 | `pet:create` |

### 1.3 页面布局设计

```
┌─────────────────────────────────────────┐
│  顶部导航栏 (AppHeader)                  │
├─────────────────────────────────────────┤
│  页面标题栏 (PageHeader)                 │
│  标题: 创建宠物                          │
│  操作: [保存草稿] [预览] [提交审核]      │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │  步骤指示器                        │  │
│  │  基本信息 → �健康状况 → 性格 → 要求│  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  步骤 1: 基本信息                  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 宠物名称 [输入框]           │  │  │
│  │  │ 物种 [下拉选择]             │  │  │
│  │  │ 品种 [输入框/自动补全]      │  │  │
│  │  │ 年龄 [年+月]                │  │  │
│  │  │ 性别 [单选按钮]             │  │  │
│  │  │ 体型 [单选按钮]             │  │  │
│  │  │ 毛色 [输入框]               │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 图片/视频上传区域            │  │  │
│  │  │ [上传图片] (3-9张)          │  │  │
│  │  │ [上传视频] (可选)           │  │  │
│  │  │ 封面图选择                   │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 地理位置                    │  │  │
│  │  │ 地址: [显示机构地址]        │  │  │
│  │  │ [修改位置]                  │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  步骤 2: 健康状况                  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 健康状态标签                │  │  │
│  │  │ [☑已绝育] [☑已疫苗] [☑已驱虫] │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 健康描述 [文本域]           │  │  │
│  │  │ （健康状况、疫苗接种情况等） │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  步骤 3: 性格特征                  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 性格描述 [文本域]           │  │  │
│  │  │ （性格特点、行为习惯等）    │  │  │
│  │  └─────────────────────────────┘  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 特征标签选择                │  │  │
│  │  │ [亲人] [活泼] [安静] [亲猫] │  │  │
│  │  │ [需陪伴] [可独处] ...        │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  步骤 4: 领养要求                  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │ 领养要求 [富文本编辑器]     │  │  │
│  │  │ （领养条件、注意事项等）    │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  底部操作栏                        │  │
│  │  [上一步] [保存草稿] [下一步]     │  │
│  │  [提交审核] (最后一步)            │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

---

## 2. 数据库模型分析

### 2.1 核心数据表

#### 2.1.1 pet（宠物档案表）

| 字段 | 类型 | 说明 | 前端用途 |
|------|------|------|---------|
| id | BIGINT | 主键ID | 宠物唯一标识 |
| org_user_id | BIGINT | 机构用户ID | 从当前登录用户获取 |
| name | VARCHAR(64) | 宠物名字 | 基本信息输入 |
| species | VARCHAR(16) | 物种：CAT/DOG/OTHER | 物种选择下拉框 |
| breed | VARCHAR(64) | 品种 | 品种输入框/自动补全 |
| gender | VARCHAR(16) | 性别：MALE/FEMALE/UNKNOWN | 性别单选按钮 |
| age_month | INT | 年龄（月） | 年龄计算（年+月） |
| size | VARCHAR(8) | 体型：S/M/L | 体型单选按钮 |
| color | VARCHAR(32) | 毛色/颜色 | 毛色输入框 |
| sterilized | TINYINT | 是否绝育：0否1是 | 健康状态复选框 |
| vaccinated | TINYINT | 是否疫苗：0否1是 | 健康状态复选框 |
| dewormed | TINYINT | 是否驱虫：0否1是 | 健康状态复选框 |
| health_desc | VARCHAR(1000) | 健康描述 | 健康描述文本域 |
| personality_desc | VARCHAR(1000) | 性格描述 | 性格描述文本域 |
| adopt_requirements | VARCHAR(1000) | 领养要求 | 领养要求富文本 |
| status | VARCHAR(16) | 状态：DRAFT/PENDING_AUDIT/PUBLISHED | 保存/提交状态 |
| audit_status | VARCHAR(16) | 审核状态：NONE/PENDING/APPROVED/REJECTED | 审核状态 |
| lng, lat | DECIMAL(10,6) | 经纬度 | 地理位置显示/编辑 |
| cover_url | VARCHAR(512) | 封面URL | 封面图选择 |
| published_time | DATETIME | 发布时间 | 审核通过后写入 |

#### 2.1.2 pet_media（宠物媒体表）

| 字段 | 类型 | 说明 | 前端用途 |
|------|------|------|---------|
| id | BIGINT | 主键ID | 唯一标识 |
| pet_id | BIGINT | 宠物ID | 关联宠物 |
| url | VARCHAR(512) | 媒体URL | 图片/视频URL |
| media_type | VARCHAR(16) | 类型：IMAGE/VIDEO | 媒体类型区分 |
| sort | INT | 排序号 | 图片排序 |

#### 2.1.3 pet_tag（宠物-标签关联表）

| 字段 | 类型 | 说明 | 前端用途 |
|------|------|------|---------|
| id | BIGINT | 主键ID | 唯一标识 |
| pet_id | BIGINT | 宠物ID | 关联宠物 |
| tag_id | BIGINT | 标签ID | 关联标签 |
| 关联 tag 表 | - | - | 获取标签名称和类型 |

#### 2.1.4 org_profile（机构资料表）

| 字段 | 类型 | 说明 | 前端用途 |
|------|------|------|---------|
| id | BIGINT | 主键ID | 唯一标识 |
| user_id | BIGINT | 机构用户ID | 关联当前用户 |
| org_name | VARCHAR(128) | 机构名称 | 机构信息显示 |
| contact_name | VARCHAR(64) | 联系人姓名 | 联系人显示 |
| contact_phone | VARCHAR(32) | 联系人电话 | 联系电话显示 |
| address | VARCHAR(255) | 详细地址 | 默认地址显示 |
| city | VARCHAR(64) | 城市 | 城市显示 |
| lng, lat | DECIMAL(10,6) | 经纬度 | 默认位置 |

#### 2.1.5 tag（标签字典表）

| 字段 | 类型 | 说明 | 前端用途 |
|------|------|------|---------|
| id | BIGINT | 主键ID | 唯一标识 |
| name | VARCHAR(64) | 标签名称 | 标签选择显示 |
| tag_type | VARCHAR(32) | 标签类型：SPECIES/PERSONALITY/HEALTH/FEATURE | 标签分类 |
| enabled | TINYINT | 是否启用：0否1是 | 筛选可用标签 |

---

## 3. 接口需求分析

### 3.1 必需接口（6个）

#### 3.1.1 创建宠物档案
- **路径**: `POST /api/org/pets`
- **权限**: `pet:create`
- **优先级**: 🔴 高（核心接口）

#### 3.1.2 公共文件上传 ⭐
- **路径**: `POST /api/upload`
- **权限**: 需要认证
- **优先级**: 🔴 高（核心功能）
- **说明**: 使用系统统一的文件上传接口，支持多种文件类型

#### 3.1.3 保存宠物媒体关联
- **路径**: `POST /api/org/pets/:id/media`
- **权限**: `pet:create`
- **优先级**: 🔴 高（核心功能）
- **说明**: 将已上传的文件URL与宠物关联

#### 3.1.4 提交审核
- **路径**: `POST /api/org/pets/:id/submit-audit`
- **权限**: `pet:submit_audit`
- **优先级**: 🔴 高（核心业务）

#### 3.1.5 获取机构信息（用于默认地址）
- **路径**: `GET /api/org/profile`
- **权限**: `org:profile:read`
- **优先级**: 🟡 中（优化体验）

#### 3.1.6 获取标签列表（用于特征选择）
- **路径**: `GET /api/tags`
- **权限**: 无需认证
- **优先级**: 🟡 中（优化体验）

### 3.2 可选接口（2个）

#### 3.2.1 更新宠物信息（编辑模式）
- **路径**: `PUT /api/org/pets/:id`
- **权限**: `pet:update`
- **优先级**: 🟡 中（编辑功能）

#### 3.2.2 删除宠物媒体
- **路径**: `DELETE /api/org/pets/:id/media/:mediaId`
- **权限**: `pet:update`
- **优先级**: 🟢 低（优化体验）

---

## 4. 页面功能设计

### 4.1 响应式数据定义

```typescript
// 表单数据
const formData = ref({
  // 基本信息
  name: '',
  species: 'CAT',          // CAT/DOG/OTHER
  breed: '',
  ageYears: 0,
  ageMonths: 0,
  gender: 'UNKNOWN',       // MALE/FEMALE/UNKNOWN
  size: 'M',               // S/M/L
  color: '',

  // 地理位置
  location: {
    address: '',
    city: '',
    district: '',
    lng: null,
    lat: null
  },

  // 健康状况
  health: {
    sterilized: false,
    vaccinated: false,
    dewormed: false,
    healthDesc: ''
  },

  // 性格特征
  personality: {
    desc: '',
    tags: []               // 选中的标签ID数组
  },

  // 领养要求
  adoptRequirements: ''
})

// 图片/视频列表
const mediaList = ref([
  {
    url: '',
    mediaType: 'IMAGE',
    sort: 0,
    isCover: false,
    isUploading: false,
    uploadProgress: 0
  }
])

// 封面图索引
const coverIndex = ref(0)

// 表单验证
const rules = ref({
  name: [
    { required: true, message: '请输入宠物名称', trigger: 'blur' },
    { min: 1, max: 64, message: '长度在1-64个字符', trigger: 'blur' }
  ],
  species: [
    { required: true, message: '请选择物种', trigger: 'change' }
  ],
  breed: [
    { required: true, message: '请输入品种', trigger: 'blur' }
  ],
  gender: [
    { required: true, message: '请选择性别', trigger: 'change' }
  ],
  size: [
    { required: true, message: '请选择体型', trigger: 'change' }
  ],
  color: [
    { required: true, message: '请输入毛色', trigger: 'blur' }
  ]
})

// 页面状态
const loading = ref(false)
const submitting = ref(false)
const currentStep = ref(1)           // 当前步骤 1-4
const petId = ref(null)              // 创建成功后的宠物ID
const isEditMode = ref(false)        // 是否为编辑模式

// 标签列表
const tagList = ref({
  PERSONALITY: [],  // 性格标签
  HEALTH: [],       // 健康标签
  FEATURE: []       // 特征标签
})

// 机构信息
const orgInfo = ref(null)
```

### 4.2 计算属性

```typescript
// 计算年龄（月）
const ageMonth = computed(() => {
  return formData.value.ageYears * 12 + formData.value.ageMonths
})

// 是否有封面图
const hasCoverImage = computed(() => {
  return mediaList.value.some(m => m.isCover && m.mediaType === 'IMAGE')
})

// 图片数量
const imageCount = computed(() => {
  return mediaList.value.filter(m => m.mediaType === 'IMAGE').length
})

// 是否可以提交
const canSubmit = computed(() => {
  // 必须有封面图
  if (!hasCoverImage.value) return false
  // 图片数量3-9张
  if (imageCount.value < 3 || imageCount.value > 9) return false
  // 基本信息必填
  if (!formData.value.name || !formData.value.breed) return false
  return true
})

// 是否显示提交审核按钮
const showSubmitButton = computed(() => {
  return currentStep.value === 4 && canSubmit.value
})

// 表单标题
const formTitle = computed(() => {
  return isEditMode.value ? '编辑宠物' : '创建宠物'
})
```

### 4.3 核心方法

```typescript
// 加载机构信息
const loadOrgInfo = async () => {
  try {
    const response = await orgAPI.getOrgProfile()
    orgInfo.value = response.data

    // 设置默认地理位置
    if (orgInfo.value.lng && orgInfo.value.lat) {
      formData.value.location = {
        address: orgInfo.value.address || '',
        city: orgInfo.value.city || '',
        district: orgInfo.value.district || '',
        lng: orgInfo.value.lng,
        lat: orgInfo.value.lat
      }
    }
  } catch (error) {
    console.error('加载机构信息失败:', error)
  }
}

// 加载标签列表
const loadTagList = async () => {
  try {
    const response = await orgAPI.getTagList()
    // 按类型分组
    response.data.list.forEach(tag => {
      if (tagList.value[tag.tagType]) {
        tagList.value[tag.tagType].push(tag)
      }
    })
  } catch (error) {
    console.error('加载标签列表失败:', error)
  }
}

// 上传图片
const uploadImage = async (file) => {
  if (imageCount.value >= 9) {
    ElMessage.warning('最多只能上传9张图片')
    return
  }

  try {
    // 步骤1: 使用公共上传接口上传文件
    const uploadFormData = new FormData()
    uploadFormData.append('file', file)
    uploadFormData.append('type', 'pet_media')

    const uploadResponse = await uploadAPI.uploadFile(uploadFormData, {
      onUploadProgress: (progressEvent) => {
        const percentCompleted = Math.round(
          (progressEvent.loaded * 100) / progressEvent.total
        )
        // 更新上传进度
      }
    })

    // 步骤2: 如果已有宠物ID，保存媒体关联信息
    let mediaId = null
    if (petId.value) {
      const mediaResponse = await orgAPI.savePetMedia(petId.value, {
        url: uploadResponse.data.url,
        mediaType: 'IMAGE',
        sort: mediaList.value.length
      })
      mediaId = mediaResponse.data.id
    }

    // 添加到媒体列表
    mediaList.value.push({
      id: mediaId,
      url: uploadResponse.data.url,
      mediaType: 'IMAGE',
      sort: mediaList.value.length,
      isCover: !hasCoverImage.value,  // 第一张图片自动设为封面
      isUploading: false,
      uploadProgress: 100
    })

    ElMessage.success('图片上传成功')
  } catch (error) {
    console.error('图片上传失败:', error)
    ElMessage.error('图片上传失败，请重试')
  }
}

// 删除媒体
const removeMedia = async (index) => {
  const media = mediaList.value[index]

  // 如果是已上传的媒体，调用删除接口
  if (media.id && petId.value) {
    try {
      await orgAPI.deletePetMedia(petId.value, media.id)
    } catch (error) {
      console.error('删除媒体失败:', error)
      return
    }
  }

  // 从列表中移除
  mediaList.value.splice(index, 1)

  // 如果删除的是封面图，设置第一张为封面
  if (media.isCover && mediaList.value.length > 0) {
    mediaList.value[0].isCover = true
    coverIndex.value = 0
  }
}

// 设置封面图
const setCoverImage = (index) => {
  mediaList.value.forEach((media, i) => {
    media.isCover = i === index
  })
  coverIndex.value = index
}

// 保存草稿
const saveDraft = async () => {
  try {
    submitting.value = true

    const data = {
      ...formData.value,
      ageMonth: ageMonth.value,
      status: 'DRAFT',
      auditStatus: 'NONE',
      lng: formData.value.location.lng,
      lat: formData.value.location.lat,
      coverUrl: mediaList.value.find(m => m.isCover)?.url || ''
    }

    let response
    if (isEditMode.value && petId.value) {
      // 更新
      response = await orgAPI.updatePet(petId.value, data)
    } else {
      // 创建
      response = await orgAPI.createPet(data)
      petId.value = response.data.id
    }

    ElMessage.success('草稿保存成功')

    // 如果是首次保存，切换到编辑模式
    if (!isEditMode.value) {
      isEditMode.value = true
      // 上传已选择的图片
      await uploadMediaList()
    }
  } catch (error) {
    console.error('保存草稿失败:', error)
    ElMessage.error('保存失败，请重试')
  } finally {
    submitting.value = false
  }
}

// 提交审核
const submitForAudit = async () => {
  // 先保存草稿
  if (!petId.value) {
    await saveDraft()
  }

  // 验证必填项
  if (!canSubmit.value) {
    ElMessage.warning('请完善所有必填信息')
    return
  }

  try {
    submitting.value = true

    await orgAPI.submitPetAudit(petId.value)

    ElMessage.success('提交审核成功')
    router.push('/org/pets')
  } catch (error) {
    console.error('提交审核失败:', error)
    ElMessage.error('提交失败，请重试')
  } finally {
    submitting.value = false
  }
}

// 上传媒体列表
const uploadMediaList = async () => {
  if (!petId.value) return

  for (let i = 0; i < mediaList.value.length; i++) {
    const media = mediaList.value[i]

    if (media.isUploading || media.id) continue  // 跳过已上传或正在上传的

    media.isUploading = true

    try {
      const formData = new FormData()
      formData.append('file', media.file)
      formData.append('type', media.mediaType)
      formData.append('sort', i)

      const response = await orgAPI.uploadPetMedia(petId.value, formData)

      media.url = response.data.url
      media.id = response.data.id
      media.isUploading = false
      media.uploadProgress = 100
    } catch (error) {
      console.error('上传媒体失败:', error)
      media.isUploading = false
    }
  }
}

// 切换标签
const toggleTag = (tag) => {
  const index = formData.value.personality.tags.indexOf(tag.id)
  if (index > -1) {
    formData.value.personality.tags.splice(index, 1)
  } else {
    formData.value.personality.tags.push(tag.id)
  }
}

// 上一步
const prevStep = () => {
  if (currentStep.value > 1) {
    currentStep.value--
  }
}

// 下一步
const nextStep = async () => {
  // 验证当前步骤
  if (!validateStep(currentStep.value)) {
    return
  }

  if (currentStep.value < 4) {
    currentStep.value++
  }
}

// 验证步骤
const validateStep = (step) => {
  if (step === 1) {
    // 验证基本信息
    if (!formData.value.name || !formData.value.breed) {
      ElMessage.warning('请填写宠物名称和品种')
      return false
    }
  }
  return true
}

// 预览
const handlePreview = () => {
  // 保存草稿
  saveDraft()

  // 跳转到预览页面
  router.push(`/org/pets/${petId.value}/preview`)
}
```

---

## 5. 接口详细设计

### 5.1 创建宠物档案

#### 接口信息
- **路径**: `POST /api/org/pets`
- **权限**: `pet:create`
- **说明**: 创建新的宠物档案

#### 请求体
```json
{
  "name": "小橘",
  "species": "CAT",
  "breed": "橘猫",
  "ageMonth": 12,
  "gender": "MALE",
  "size": "M",
  "color": "橘白色",
  "sterilized": 1,
  "vaccinated": 1,
  "dewormed": 1,
  "healthDesc": "健康状况良好，已完成基础疫苗",
  "personalityDesc": "性格温顺，亲人",
  "adoptRequirements": "需要有固定住所，定期回访",
  "lng": 116.404,
  "lat": 39.915,
  "coverUrl": "https://example.com/pet1-cover.jpg",
  "status": "DRAFT"
}
```

#### 响应数据
```json
{
  "code": 10000,
  "message": "创建成功",
  "data": {
    "id": 100,
    "name": "小橘",
    "status": "DRAFT",
    "createTime": "2024-01-20T10:00:00"
  }
}
```

#### 业务规则
- 创建时默认状态为 `DRAFT`（草稿）
- `org_user_id` 从当前登录用户获取
- 封面图URL可以为空，首次上传后更新

---

### 5.2 公共文件上传接口 ⭐

#### 接口信息
- **路径**: `POST /api/upload`
- **权限**: 需要认证
- **说明**: 系统统一的文件上传接口，支持多种文件类型

#### 请求体（multipart/form-data）
| 参数 | 类型 | 必填 | 说明 |
|------|------|-----|------|
| file | File | 是 | 要上传的文件 |
| type | String | 是 | 文件类型标识：avatar/pet_media/id_card/license/other |

#### 响应数据
```json
{
  "code": 10000,
  "message": "上传成功",
  "data": {
    "url": "https://example.com/files/pet1-1.jpg",
    "filename": "pet1-1.jpg",
    "size": 2048576,
    "mime": "image/jpeg"
  }
}
```

#### 业务规则
- 支持格式：jpg, png, gif, mp4等
- 文件大小：根据文件类型限制
- 返回文件URL供后续使用

---

### 5.3 保存宠物媒体关联

### 5.4 提交审核
- **路径**: `POST /api/org/pets/:id/media`
- **权限**: `pet:create`
- **说明**: 将已上传的文件URL与宠物关联，保存到pet_media表

#### 请求体
```json
{
  "url": "https://example.com/files/pet1-1.jpg",
  "mediaType": "IMAGE",
  "sort": 1,
  "isCover": false
}
```

#### 响应数据
```json
{
  "code": 10000,
  "message": "保存成功",
  "data": {
    "id": 1000,
    "url": "https://example.com/files/pet1-1.jpg",
    "mediaType": "IMAGE",
    "sort": 1
  }
}
```

#### 业务规则
- 图片限制：3-9张
- 视频限制：0-1个（可选）
- mediaType：IMAGE/VIDEO
### 5.5 获取机构信息制图片显示顺序
- isCover：是否为封面图（通常在创建时第一张自动设为封面）

#### 文件上传流程
```
1. 用户选择文件
   ↓
2. 调用 POST /api/upload 上传文件（type: "pet_media"）
   ↓
3. 获取文件URL
   ↓
4. 调用 POST /api/org/pets/:id/media 保存媒体关联信息
   ↓
5. 更新pet表cover_url字段（如果是封面图）
```

---

### 5.3 提交审核

#### 接口信息
- **路径**: `POST /api/org/pets/:id/submit-audit`
- **权限**: `pet:submit_audit`
- **说明**: 提交宠物档案到管理员审核

#### 请求体
```json
{}
```

### 5.6 获取标签列表
```json
{
  "code": 10000,
  "message": "提交成功",
  "data": {
    "petId": 100,
    "status": "PENDING_AUDIT",
    "auditStatus": "PENDING"
  }
}
```

#### 业务规则
- 只有 `DRAFT` 状态的宠物才能提交审核
- 提交后状态变更为 `PENDING_AUDIT`
- 审核状态变更为 `PENDING`
- 必须有封面图且至少3张图片

---

### 5.4 获取机构信息

#### 接口信息
- **路径**: `GET /api/org/profile`
- **权限**: `org:profile:read`
- **说明**: 获取当前登录机构的基本信息

#### 响应数据
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": {
    "id": 1,
    "userId": 10,
    "orgName": "爱心救助站",
    "contactName": "张三",
    "contactPhone": "13800000001",
    "address": "北京市朝阳区xxx路xxx号",
    "province": "北京市",
    "city": "北京市",
    "district": "朝阳区",
    "lng": 116.404,
    "lat": 39.915
### 5.7 更新宠物信息（编辑模式）
}
```

---

### 5.5 获取标签列表

#### 接口信息
- **路径**: `GET /api/tags`
- **权限**: 无需认证
- **说明**: 获取标签字典列表

#### 请求参数
| 参数 | 类型 | 必填 | 说明 |
|------|------|-----|------|
| tagType | query | N | 标签类型：PERSONALITY/HEALTH/FEATURE |
| enabled | query | N | 是否启用：0/1，默认1 |

#### 响应数据
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 1,
        "name": "亲人",
        "tagType": "PERSONALITY",
        "enabled": 1
      },
      {
        "id": 2,
        "name": "活泼",
        "tagType": "PERSONALITY",
        "enabled": 1
      },
      {
        "id": 10,
        "name": "已绝育",
        "tagType": "HEALTH",
        "enabled": 1
### 5.8 删除宠物媒体
    ]
  }
}
```

---

### 5.6 更新宠物信息（编辑模式）

#### 接口信息
- **路径**: `PUT /api/org/pets/:id`
- **权限**: `pet:update`
- **说明**: 更新宠物档案信息

#### 请求体
```json
{
  "name": "小橘",
  "species": "CAT",
  "breed": "橘猫",
  "ageMonth": 12,
  "gender": "MALE",
  "size": "M",
  "color": "橘白色",
  "sterilized": 1,
  "vaccinated": 1,
  "dewormed": 1,
  "healthDesc": "健康状况良好",
  "personalityDesc": "性格温顺",
  "adoptRequirements": "需要有固定住所",
  "lng": 116.404,
  "lat": 39.915,
  "coverUrl": "https://example.com/pet1-cover.jpg"
}
```

#### 响应数据
```json
{
  "code": 10000,
  "message": "更新成功"
}
```

#### 业务规则
- 只有 `DRAFT` 或 `PENDING_AUDIT` 状态的宠物可以编辑
- `PUBLISHED` 状态的宠物不能直接编辑，需要先下架

---

### 5.7 删除宠物媒体

#### 接口信息
- **路径**: `DELETE /api/org/pets/:id/media/:mediaId`
- **权限**: `pet:update`
- **说明**: 删除指定的宠物媒体文件

#### 响应数据
```json
{
  "code": 10000,
  "message": "删除成功"
}
```

#### 业务规则
- 软删除，更新 `deleted` 字段为1
- 不能删除唯一的一张封面图（需先设置其他图片为封面）

---

## 6. 开发任务分解

### 6.1 Phase 1: 基础表单（核心功能）

#### Task 1.1: API 接口开发
- [ ] 实现 `POST /api/org/pets` 接口
- [ ] 实现 `GET /api/org/profile` 接口
- [ ] 实现 `GET /api/tags` 接口
- [ ] 添加接口文档和 JSDoc 注释

#### Task 1.2: 前端页面框架
- [ ] 创建 PetCreate.vue 页面组件
#### Task 2.1: API 接口开发
- [ ] 实现 `POST /api/org/pets/:id/media` 接口（保存媒体关联）
- [ ] 验证公共上传接口 `POST /api/upload` 是否可用
- [ ] 实现媒体排序逻辑
- [ ] 实现封面图设置逻辑3: 样式开发
- [ ] 实现响应式布局（移动端优先）
- [ ] 实现步骤指示器样式
- [ ] 实现表单样式

---

### 6.2 Phase 2: 媒体上传（核心功能）

#### Task 2.1: API 接口开发
- [ ] 实现 `POST /api/org/pets/:id/media` 接口
- [ ] 实现文件上传功能
- [ ] 实现文件格式和大小验证
- [ ] 实现媒体排序

#### Task 2.2: 前端上传组件
- [ ] 实现图片上传区域（支持拖拽）
- [ ] 实现图片预览
- [ ] 实现封面图选择
- [ ] 实现图片排序（拖拽或按钮）
- [ ] 实现图片删除
- [ ] 实现上传进度显示

#### Task 2.3: 样式开发
- [ ] 实现上传区域样式
- [ ] 实现图片预览样式
- [ ] 实现封面图标记样式

---

### 6.3 Phase 3: 完整表单（核心功能）

#### Task 3.1: 健康状况表单（步骤2）
- [ ] 实现健康状态复选框（已绝育/已疫苗/已驱虫）
- [ ] 实现健康描述文本域
- [ ] 集成表单验证

#### Task 3.2: 性格特征表单（步骤3）
- [ ] 实现性格描述文本域
- [ ] 实现特征标签选择（多选）
- [ ] 实现标签分组展示
- [ ] 集成表单验证

#### Task 4.1: API 接口开发
- [ ] 实现 `PUT /api/org/pets/:id` 接口
- [ ] 实现 `POST /api/org/pets/:id/submit-audit` 接口
- [ ] 实现 `DELETE /api/org/pets/:id/media/:mediaId` 接口
- [ ] 集成公共上传接口 `POST /api/upload`
#### Task 3.4: 地理位置组件
- [ ] 实现地址显示
- [ ] 实现位置修改功能（地图选择）
- [ ] 集成高德地图API

---

### 6.4 Phase 4: 提交与审核（核心业务）

#### Task 4.1: API 接口开发
- [ ] 实现 `PUT /api/org/pets/:id` 接口
- [ ] 实现 `POST /api/org/pets/:id/submit-audit` 接口
- [ ] 实现 `DELETE /api/org/pets/:id/media/:mediaId` 接口

#### Task 4.2: 前端提交逻辑
- [ ] 实现保存草稿功能
- [ ] 实现提交审核功能
- [ ] 实现表单完整性验证
- [ ] 实现提交前必填项检查

#### Task 4.3: 用户体验优化
- [ ] 添加加载状态提示
- [ ] 添加成功/失败提示
- [ ] 添加确认对话框（提交审核）

---

/**
 * 保存宠物媒体关联（不包含文件上传）
 * @param {number} petId - 宠物ID
 * @param {object} data - 媒体关联信息
 * @returns {Promise} 保存结果
 */
export function savePetMedia(petId, data) {
  return http.post(`/org/pets/${petId}/media`, data)
}

/**
 * 删除宠物媒体
 * @param {number} petId - 宠物ID
 * @param {number} mediaId - 媒体ID
 * @returns {Promise} 删除结果
 */
export function deletePetMedia(petId, mediaId) {
  return http.delete(`/org/pets/${petId}/media/${mediaId}`)
} [ ] 实现媒体替换功能
- [ ] 实现媒体删除功能

---

### 6.6 Phase 6: 优化与测试

#### Task 6.1: 性能优化
- [ ] 图片压缩上传
- [ ] 图片懒加载
- [ ] 表单数据缓存（防止刷新丢失）

import http from '@/api/request.js'
import { upload as uploadAPI } from '@/api/modules/upload.js'

/**
 * 创建宠物档案
 * @param {object} data - 宠物信息
 * @returns {Promise} 创建结果
 */
export function createPet(data) {
  return http.post('/org/pets', data)
} [ ] 端到端测试（完整流程）

---

## 7. 技术实现要点

### 7.1 API 接口实现

#### 文件位置
`src/api/modules/org.js`

#### 接口方法
```javascript
import http from '@/api/request.js'

/**
 * 创建宠物档案
 * @param {object} data - 宠物信息
 * @returns {Promise} 创建结果
 */
export function createPet(data) {
  return http.post('/org/pets', data)
}

/**
 * 更新宠物信息
 * @param {number} id - 宠物ID
 * @param {object} data - 宠物信息
 * @returns {Promise} 更新结果
 */
export function updatePet(id, data) {
  return http.put(`/org/pets/${id}`, data)
}

/**
 * 上传宠物媒体
 * @param {number} petId - 宠物ID
 * @param {FormData} formData - 表单数据
 * @param {object} config - 上传配置（onUploadProgress）
 * @returns {Promise} 上传结果
 */
export function uploadPetMedia(petId, formData, config = {}) {
  return http.upload(`/org/pets/${petId}/media`, formData, config)
}

/**
 * 删除宠物媒体
 * @param {number} petId - 宠物ID
 * @param {number} mediaId - 媒体ID
 * @returns {Promise} 删除结果
 */
export function deletePetMedia(petId, mediaId) {
  return http.delete(`/org/pets/${petId}/media/${mediaId}`)
}

/**
 * 提交审核
 * @param {number} id - 宠物ID
 * @returns {Promise} 提交结果
 */
export function submitPetAudit(id) {
  return http.post(`/org/pets/${id}/submit-audit`)
}

/**
 * 获取标签列表
 * @param {object} params - 查询参数
 * @returns {Promise} 标签列表
 */
export function getTagList(params = {}) {
  return http.get('/tags', params)
}
```

### 7.2 前端组件结构

#### PetCreate.vue 完整结构
```vue
<template>
  <AppLayout>
    <PageHeader :title="formTitle">
      <template #actions>
        <el-button @click="saveDraft" :loading="submitting">
          保存草稿
        </el-button>
        <el-button @click="handlePreview">
          预览
        </el-button>
        <el-button
          type="primary"
          @click="submitForAudit"
          :loading="submitting"
          :disabled="!canSubmit"
        >
          提交审核
        </el-button>
      </template>
    </PageHeader>

    <div class="pet-create-container">
      <!-- 步骤指示器 -->
      <el-steps :active="currentStep" align-center>
        <el-step title="基本信息" />
        <el-step title="健康状况" />
        <el-step title="性格特征" />
        <el-step title="领养要求" />
      </el-steps>

      <el-form
        ref="formRef"
        :model="formData"
        :rules="rules"
        label-width="100px"
      >
        <!-- 步骤1: 基本信息 -->
        <div v-show="currentStep === 1" class="step-content">
          <!-- 基本信息表单 -->
          <el-form-item label="宠物名称" prop="name">
            <el-input v-model="formData.name" placeholder="请输入宠物名称" />
          </el-form-item>

          <el-form-item label="物种" prop="species">
            <el-radio-group v-model="formData.species">
              <el-radio label="CAT">猫咪</el-radio>
              <el-radio label="DOG">狗狗</el-radio>
              <el-radio label="OTHER">其他</el-radio>
            </el-radio-group>
          </el-form-item>

          <el-form-item label="品种" prop="breed">
            <el-input v-model="formData.breed" placeholder="请输入品种" />
          </el-form-item>

          <el-form-item label="年龄">
            <el-col :span="11">
              <el-input-number
                v-model="formData.ageYears"
                :min="0"
                :max="30"
                placeholder="年"
              />
            </el-col>
            <el-col :span="2" class="text-center">年</el-col>
            <el-col :span="11">
              <el-input-number
                v-model="formData.ageMonths"
                :min="0"
                :max="11"
                placeholder="月"
              />
            </el-col>
            <el-col :span="2" class="text-center">月</el-col>
          </el-form-item>

          <el-form-item label="性别" prop="gender">
            <el-radio-group v-model="formData.gender">
              <el-radio label="MALE">男孩</el-radio>
              <el-radio label="FEMALE">女孩</el-radio>
              <el-radio label="UNKNOWN">未知</el-radio>
            </el-radio-group>
          </el-form-item>

          <el-form-item label="体型" prop="size">
            <el-radio-group v-model="formData.size">
              <el-radio label="S">小型</el-radio>
              <el-radio label="M">中型</el-radio>
              <el-radio label="L">大型</el-radio>
            </el-radio-group>
          </el-form-item>

          <el-form-item label="毛色" prop="color">
            <el-input v-model="formData.color" placeholder="请输入毛色" />
          </el-form-item>

          <!-- 地理位置 -->
          <el-form-item label="所在位置">
            <el-input
              v-model="formData.location.address"
              placeholder="显示机构地址"
              readonly
            >
              <template #append>
                <el-button @click="editLocation">修改</el-button>
              </template>
            </el-input>
          </el-form-item>

          <!-- 图片上传 -->
          <el-form-item label="宠物照片">
            <MediaUploader
              :media-list="mediaList"
              @upload="uploadImage"
              @remove="removeMedia"
              @set-cover="setCoverImage"
            />
          </el-form-item>
        </div>

        <!-- 步骤2: 健康状况 -->
        <div v-show="currentStep === 2" class="step-content">
          <el-form-item label="健康状态">
            <el-checkbox-group v-model="healthStatus">
              <el-checkbox label="已绝育" />
              <el-checkbox label="已疫苗" />
              <el-checkbox label="已驱虫" />
            </el-checkbox-group>
          </el-form-item>

          <el-form-item label="健康描述">
            <el-input
              v-model="formData.health.healthDesc"
              type="textarea"
              :rows="4"
              placeholder="请描述宠物的健康状况、疫苗接种情况等"
            />
          </el-form-item>
        </div>

        <!-- 步骤3: 性格特征 -->
        <div v-show="currentStep === 3" class="step-content">
          <el-form-item label="性格描述">
            <el-input
              v-model="formData.personality.desc"
              type="textarea"
              :rows="4"
              placeholder="请描述宠物的性格特点、行为习惯等"
            />
          </el-form-item>

          <el-form-item label="特征标签">
            <TagSelector
              :tag-list="tagList"
              :selected="formData.personality.tags"
              @toggle="toggleTag"
            />
          </el-form-item>
        </div>

        <!-- 步骤4: 领养要求 -->
        <div v-show="currentStep === 4" class="step-content">
          <el-form-item label="领养要求">
            <el-input
              v-model="formData.adoptRequirements"
              type="textarea"
              :rows="6"
              placeholder="请描述领养条件、注意事项等"
            />
          </el-form-item>
        </div>
      </el-form>

      <!-- 底部操作栏 -->
      <div class="bottom-actions">
        <el-button
          v-if="currentStep > 1"
          @click="prevStep"
        >
          上一步
        </el-button>

        <el-button
          v-if="currentStep < 4"
          type="primary"
          @click="nextStep"
        >
          下一步
        </el-button>
      </div>
    </div>
  </AppLayout>
</template>

<script setup>
// ... 代码实现
</script>

<style scoped>
/* ... 样式实现 */
</style>
```

### 7.3 子组件设计

#### MediaUploader.vue（媒体上传组件）
```vue
<template>
  <div class="media-uploader">
    <!-- 封面图显示 -->
    <div class="cover-section">
      <div class="cover-label">封面图</div>
      <div class="cover-image">
        <el-image
          v-if="coverMedia"
          :src="coverMedia.url"
          fit="cover"
        />
        <div v-else class="empty-cover">
          请选择封面图
        </div>
      </div>
    </div>

    <!-- 图片列表 -->
    <div class="image-list">
      <div
        v-for="(media, index) in imageList"
        :key="index"
        class="image-item"
        :class="{ 'is-cover': media.isCover }"
      >
        <el-image :src="media.url" fit="cover" />
        <div class="image-overlay">
          <el-button
            size="small"
            @click="setCover(index)"
          >
            设为封面
          </el-button>
          <el-button
            size="small"
            type="danger"
            @click="remove(index)"
          >
            删除
          </el-button>
        </div>
        <div v-if="media.isCover" class="cover-badge">封面</div>
      </div>

      <!-- 上传按钮 -->
      <el-upload
        v-if="imageList.length < 9"
        class="upload-item"
        :show-file-list="false"
        :before-upload="handleBeforeUpload"
        :http-request="handleUpload"
      >
        <el-icon><Plus /></el-icon>
      </el-upload>
    </div>

    <!-- 提示信息 -->
    <div class="upload-tips">
      请上传3-9张宠物照片，支持jpg、png、gif格式，单张不超过5MB
    </div>
  </div>
</template>
```

#### TagSelector.vue（标签选择组件）
```vue
<template>
  <div class="tag-selector">
    <div
      v-for="(tags, type) in groupedTags"
      :key="type"
      class="tag-group"
    >
      <div class="group-title">{{ getTypeName(type) }}</div>
      <div class="tag-list">
        <el-tag
          v-for="tag in tags"
          :key="tag.id"
          :type="isSelected(tag.id) ? 'primary' : ''"
          @click="toggle(tag)"
        >
          {{ tag.name }}
        </el-tag>
      </div>
    </div>
  </div>
</template>
```

### 7.4 样式要点

#### 响应式设计
```scss
.pet-create-container {
  padding: 0 16px;
  max-width: 800px;
  margin: 0 auto;
}

@media (min-width: 768px) {
  .pet-create-container {
    padding: 0 24px;
    max-width: 1000px;
  }
}

// 步骤内容
.step-content {
  margin-top: var(--spacing-xl);
}

// 底部操作栏
.bottom-actions {
  display: flex;
  justify-content: center;
  gap: var(--spacing-md);
  margin-top: var(--spacing-xxl);
  padding-bottom: var(--spacing-xxl);
}
```

#### 媒体上传样式
```scss
.media-uploader {
  .cover-section {
    margin-bottom: var(--spacing-lg);

    .cover-image {
      width: 100%;
      height: 300px;
      border-radius: var(--border-radius-large);
      overflow: hidden;
      background: var(--bg-light);
      display: flex;
      align-items: center;
      justify-content: center;
    }
  }

  .image-list {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
    gap: var(--spacing-md);
  }

  .image-item {
    position: relative;
    aspect-ratio: 1;
    border-radius: var(--border-radius-medium);
    overflow: hidden;

    &.is-cover {
      border: 2px solid var(--primary-color);
    }

    .image-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: rgba(0, 0, 0, 0.5);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--spacing-xs);
      opacity: 0;
      transition: opacity 0.3s;
    }

    &:hover .image-overlay {
      opacity: 1;
    }
  }
}
```

### 7.5 性能优化

#### 图片压缩
```javascript
const compressImage = async (file) => {
  return new Promise((resolve, reject) => {
    const reader = new FileReader()
    reader.onload = (e) => {
      const img = new Image()
      img.onload = () => {
        const canvas = document.createElement('canvas')
        const ctx = canvas.getContext('2d')

        // 计算压缩后的尺寸
        let width = img.width
        let height = img.height
        const maxSize = 1920

        if (width > maxSize || height > maxSize) {
          const ratio = Math.min(maxSize / width, maxSize / height)
          width *= ratio
          height *= ratio
        }

        canvas.width = width
        canvas.height = height
        ctx.drawImage(img, 0, 0, width, height)

        // 压缩并导出
        canvas.toBlob(
          (blob) => {
            resolve(new File([blob], file.name, { type: 'image/jpeg' }))
          },
          'image/jpeg',
          0.8
        )
      }
      img.onerror = reject
      img.src = e.target.result
    }
    reader.onerror = reject
    reader.readAsDataURL(file)
  })
}
```

#### 表单数据缓存
```javascript
const saveFormDataToStorage = () => {
  const data = {
    formData: formData.value,
    mediaList: mediaList.value,
    currentStep: currentStep.value
  }
  localStorage.setItem('pet_create_draft', JSON.stringify(data))
}

const loadFormDataFromStorage = () => {
  const data = localStorage.getItem('pet_create_draft')
  if (data) {
    const parsed = JSON.parse(data)
    formData.value = parsed.formData
    mediaList.value = parsed.mediaList
    currentStep.value = parsed.currentStep
  }
}
```

---

## 8. 总结

### 8.1 开发优先级

| 优先级 | 功能模块 | 说明 |
|--------|---------|------|
| P0 | 创建宠物档案 | 核心接口，必须优先实现 |
| P0 | 上传宠物媒体 | 核心功能，必须优先实现 |
| P0 | 提交审核 | 核心业务，必须优先实现 |
| P1 | 基本信息表单 | 核心功能，必须优先实现 |
| P1 | 健康状况表单 | 核心功能，必须优先实现 |
| P1 | 性格特征表单 | 核心功能，必须优先实现 |
| P1 | 领养要求表单 | 核心功能，必须优先实现 |
| P2 | 编辑模式 | 优化体验，可延后实现 |
| P2 | 自动保存 | 优化体验，可延后实现 |
| P3 | 图片压缩 | 性能优化，可选实现 |

### 8.2 技术风险

| 风险项 | 风险等级 | 解决方案 |
|--------|---------|---------|
| 大文件上传性能 | 中 | 使用分片上传 + 图片压缩 |
| 表单数据丢失 | 中 | 实现自动保存 + 本地缓存 |
| 图片顺序管理 | 低 | 使用拖拽排序库（sortablejs） |
| 富文本编辑器兼容性 | 低 | 使用成熟的库（quill/tinymce） |

### 8.3 后续优化方向

1. **图片智能裁剪**：AI自动识别宠物主体
2. **自动生成描述**：基于图片信息自动生成描述
3. **批量导入**：Excel批量导入宠物信息
4. **模板功能**：保存常用模板，快速创建
5. **AI标签推荐**：基于图片自动推荐特征标签

---

**文档版本**: v1.0
**最后更新**: 2026-02-15
**负责人**: 开发团队