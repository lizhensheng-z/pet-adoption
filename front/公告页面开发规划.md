# 公告页面开发规划文档

> 本文档基于系统公告表(sys_notice)制定，提供公告管理功能的完整前后端接口规范，确保前后端按照统一标准开发。

---

## 1. 数据库表结构分析

### 1.1 系统公告表(sys_notice)

```sql
CREATE TABLE sys_notice (
    id BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    title VARCHAR(128) NOT NULL COMMENT '公告标题',
    content TEXT NOT NULL COMMENT '公告内容',
    status VARCHAR(16) NOT NULL DEFAULT 'PUBLISHED' COMMENT '公告状态：DRAFT/PUBLISHED/REMOVED',
    create_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_status(status) COMMENT '公告状态索引'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统公告表';
```

### 1.2 字段说明

| 字段名 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| id | BIGINT | 公告唯一标识 | 1 |
| title | VARCHAR(128) | 公告标题 | "系统维护通知" |
| content | TEXT | 公告内容 | "将于今晚22:00-23:00进行系统维护..." |
| status | VARCHAR(16) | 公告状态 | DRAFT/PUBLISHED/REMOVED |
| create_time | DATETIME | 创建时间 | 2024-02-16 10:00:00 |
| update_time | DATETIME | 更新时间 | 2024-02-16 10:30:00 |

---

## 2. 前端页面规划

### 2.1 管理端公告管理页面

**页面路径**: `/admin/notices`
**组件路径**: `src/views/admin/Notices.vue`
**权限要求**: 管理员权限

#### 2.1.1 页面功能
- 公告列表展示（分页）
- 公告搜索（按标题、状态）
- 新增公告
- 编辑公告
- 删除公告（软删除）
- 发布/下架公告
- 公告详情查看

#### 2.1.2 页面布局
```
┌─────────────────────────────────────────┐
│ 公告管理                    [新增公告]    │
├─────────────────────────────────────────┤
│ 搜索区域：[标题输入框] [状态下拉框] [搜索] │
├─────────────────────────────────────────┤
│ ┌─────┬──────────┬──────────┬─────────┐ │
│ │ ID  │ 标题     │ 状态     │ 操作    │ │
│ ├─────┼──────────┼──────────┼─────────┤ │
│ │ 1   │ 维护通知 │ 已发布   │ [编辑]  │ │
│ │ 2   │ 更新公告 │ 草稿     │ [发布]  │ │
│ └─────┴──────────┴──────────┴─────────┘ │
│ 分页：[上一页] 1 2 3 [下一页]            │
└─────────────────────────────────────────┘
```

### 2.2 用户端公告展示

**页面路径**: `/notices`
**组件路径**: `src/views/user/NoticeList.vue`
**权限要求**: 所有用户可见

#### 2.2.1 页面功能
- 公告列表展示
- 公告详情查看
- 最新公告标记

---

## 3. 后端接口规范

### 3.1 管理端接口

#### 3.1.1 获取公告列表（分页）

**接口说明**: 获取公告列表，支持分页和筛选

**请求方式**: `GET`

**请求路径**: `/api/admin/notices`

**请求头**:
```
Authorization: Bearer <access_token>
Content-Type: application/json
```

**请求参数**:

| 参数 | 类型 | 必填 | 说明 | 默认值 |
|------|------|------|------|--------|
| pageNo | Integer | 否 | 页码 | 1 |
| pageSize | Integer | 否 | 每页条数 | 10 |
| title | String | 否 | 公告标题模糊搜索 | - |
| status | String | 否 | 公告状态筛选 | - |

**请求示例**:
```
GET /api/admin/notices?pageNo=1&pageSize=10&title=维护&status=PUBLISHED
```

**响应示例**:
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "系统维护通知",
        "content": "系统将于今晚22:00-23:00进行维护...",
        "status": "PUBLISHED",
        "statusText": "已发布",
        "createTime": "2024-02-16 10:00:00",
        "updateTime": "2024-02-16 10:30:00"
      }
    ],
    "pageNo": 1,
    "pageSize": 10,
    "total": 15,
    "totalPages": 2
  }
}
```

#### 3.1.2 获取公告详情

**接口说明**: 根据ID获取公告详情

**请求方式**: `GET`

**请求路径**: `/api/admin/notices/{id}`

**请求示例**:
```
GET /api/admin/notices/1
```

**响应示例**:
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": {
    "id": 1,
    "title": "系统维护通知",
    "content": "系统将于今晚22:00-23:00进行维护...",
    "status": "PUBLISHED",
    "createTime": "2024-02-16 10:00:00",
    "updateTime": "2024-02-16 10:30:00"
  }
}
```

#### 3.1.3 创建公告

**接口说明**: 创建新公告

**请求方式**: `POST`

**请求路径**: `/api/admin/notices`

**请求体**:
```json
{
  "title": "系统维护通知",
  "content": "系统将于今晚22:00-23:00进行维护...",
  "status": "DRAFT"
}
```

**响应示例**:
```json
{
  "code": 10000,
  "message": "创建成功",
  "data": {
    "id": 2,
    "title": "系统维护通知",
    "content": "系统将于今晚22:00-23:00进行维护...",
    "status": "DRAFT",
    "createTime": "2024-02-16 11:00:00",
    "updateTime": "2024-02-16 11:00:00"
  }
}
```

#### 3.1.4 更新公告

**接口说明**: 更新公告信息

**请求方式**: `PUT`

**请求路径**: `/api/admin/notices/{id}`

**请求体**:
```json
{
  "title": "系统维护通知（更新）",
  "content": "系统将于今晚22:00-23:00进行维护，请提前做好准备...",
  "status": "PUBLISHED"
}
```

**响应示例**:
```json
{
  "code": 10000,
  "message": "更新成功",
  "data": null
}
```

#### 3.1.5 删除公告

**接口说明**: 软删除公告（状态改为REMOVED）

**请求方式**: `DELETE`

**请求路径**: `/api/admin/notices/{id}`

**响应示例**:
```json
{
  "code": 10000,
  "message": "删除成功",
  "data": null
}
```

#### 3.1.6 发布/下架公告

**接口说明**: 修改公告状态

**请求方式**: `PATCH`

**请求路径**: `/api/admin/notices/{id}/status`

**请求体**:
```json
{
  "status": "PUBLISHED"
}
```

**响应示例**:
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": null
}
```

### 3.2 用户端接口

#### 3.2.1 获取已发布公告列表

**接口说明**: 获取所有已发布的公告列表

**请求方式**: `GET`

**请求路径**: `/api/notices`

**请求参数**:

| 参数 | 类型 | 必填 | 说明 | 默认值 |
|------|------|------|------|--------|
| pageNo | Integer | 否 | 页码 | 1 |
| pageSize | Integer | 否 | 每页条数 | 10 |

**响应示例**:
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": {
    "list": [
      {
        "id": 1,
        "title": "系统维护通知",
        "content": "系统将于今晚22:00-23:00进行维护...",
        "createTime": "2024-02-16 10:00:00",
        "isNew": true
      }
    ],
    "pageNo": 1,
    "pageSize": 10,
    "total": 5,
    "totalPages": 1
  }
}
```

#### 3.2.2 获取公告详情

**接口说明**: 获取公告详情

**请求方式**: `GET`

**请求路径**: `/api/notices/{id}`

**响应示例**:
```json
{
  "code": 10000,
  "message": "操作成功",
  "data": {
    "id": 1,
    "title": "系统维护通知",
    "content": "系统将于今晚22:00-23:00进行维护...",
    "createTime": "2024-02-16 10:00:00"
  }
}
```

---

## 4. 前端实现规范

### 4.1 API模块定义

**文件路径**: `src/api/modules/admin.js`

```javascript
// 公告管理相关API
export const noticeApi = {
  // 获取公告列表
  getNoticeList: (params) => http.get('/admin/notices', params),
  
  // 获取公告详情
  getNoticeDetail: (id) => http.get(`/admin/notices/${id}`),
  
  // 创建公告
  createNotice: (data) => http.post('/admin/notices', data),
  
  // 更新公告
  updateNotice: (id, data) => http.put(`/admin/notices/${id}`, data),
  
  // 删除公告
  deleteNotice: (id) => http.delete(`/admin/notices/${id}`),
  
  // 更新公告状态
  updateNoticeStatus: (id, status) => 
    http.patch(`/admin/notices/${id}/status`, { status })
}

// 用户端公告API
export const userNoticeApi = {
  // 获取公告列表
  getNoticeList: (params) => http.get('/notices', params),
  
  // 获取公告详情
  getNoticeDetail: (id) => http.get(`/notices/${id}`)
}
```

### 4.2 状态管理

**文件路径**: `src/stores/notice.js`

```javascript
import { defineStore } from 'pinia'
import { noticeApi, userNoticeApi } from '@/api/modules/admin.js'

export const useNoticeStore = defineStore('notice', {
  state: () => ({
    noticeList: [],
    total: 0,
    currentNotice: null,
    loading: false
  }),
  
  actions: {
    // 获取公告列表
    async fetchNoticeList(params = {}) {
      this.loading = true
      try {
        const response = await noticeApi.getNoticeList(params)
        if (response.code === 10000) {
          this.noticeList = response.data.list
          this.total = response.data.total
        }
      } finally {
        this.loading = false
      }
    },
    
    // 获取公告详情
    async fetchNoticeDetail(id) {
      const response = await noticeApi.getNoticeDetail(id)
      if (response.code === 10000) {
        this.currentNotice = response.data
      }
    },
    
    // 创建公告
    async createNotice(data) {
      const response = await noticeApi.createNotice(data)
      return response.code === 10000
    },
    
    // 更新公告
    async updateNotice(id, data) {
      const response = await noticeApi.updateNotice(id, data)
      return response.code === 10000
    },
    
    // 删除公告
    async deleteNotice(id) {
      const response = await noticeApi.deleteNotice(id)
      return response.code === 10000
    }
  }
})
```

### 4.3 页面组件实现

#### 4.3.1 管理端公告管理页面

**文件路径**: `src/views/admin/Notices.vue`

```vue
<template>
  <AppLayout>
    <PageHeader title="公告管理">
      <template #actions>
        <el-button type="primary" @click="handleAdd">
          <el-icon><Plus /></el-icon>
          新增公告
        </el-button>
      </template>
    </PageHeader>

    <div class="page-container">
      <!-- 搜索区域 -->
      <el-card class="search-card">
        <el-form :inline="true" :model="searchForm">
          <el-form-item label="公告标题">
            <el-input
              v-model="searchForm.title"
              placeholder="请输入公告标题"
              clearable
            />
          </el-form-item>
          <el-form-item label="状态">
            <el-select v-model="searchForm.status" placeholder="全部" clearable>
              <el-option label="草稿" value="DRAFT" />
              <el-option label="已发布" value="PUBLISHED" />
              <el-option label="已删除" value="REMOVED" />
            </el-select>
          </el-form-item>
          <el-form-item>
            <el-button type="primary" @click="handleSearch">搜索</el-button>
            <el-button @click="handleReset">重置</el-button>
          </el-form-item>
        </el-form>
      </el-card>

      <!-- 列表区域 -->
      <el-card>
        <el-table
          :data="noticeList"
          v-loading="loading"
          stripe
          style="width: 100%"
        >
          <el-table-column prop="id" label="ID" width="80" />
          <el-table-column prop="title" label="公告标题" min-width="200" />
          <el-table-column prop="statusText" label="状态" width="100">
            <template #default="{ row }">
              <el-tag :type="getStatusType(row.status)">
                {{ row.statusText }}
              </el-tag>
            </template>
          </el-table-column>
          <el-table-column prop="createTime" label="创建时间" width="180" />
          <el-table-column label="操作" width="200" fixed="right">
            <template #default="{ row }">
              <el-button type="primary" link @click="handleEdit(row)">
                编辑
              </el-button>
              <el-button
                v-if="row.status === 'DRAFT'"
                type="success"
                link
                @click="handlePublish(row)"
              >
                发布
              </el-button>
              <el-button
                v-if="row.status === 'PUBLISHED'"
                type="warning"
                link
                @click="handleUnpublish(row)"
              >
                下架
              </el-button>
              <el-button type="danger" link @click="handleDelete(row)">
                删除
              </el-button>
            </template>
          </el-table-column>
        </el-table>

        <!-- 分页 -->
        <el-pagination
          v-model:current-page="pagination.pageNo"
          v-model:page-size="pagination.pageSize"
          :total="total"
          :page-sizes="[10, 20, 50, 100]"
          layout="total, sizes, prev, pager, next, jumper"
          @size-change="handleSizeChange"
          @current-change="handlePageChange"
        />
      </el-card>
    </div>

    <!-- 新增/编辑对话框 -->
    <NoticeFormDialog
      v-model="dialogVisible"
      :notice="currentNotice"
      @success="handleSuccess"
    />
  </AppLayout>
</template>

<script setup>
import { ref, reactive, onMounted } from 'vue'
import { ElMessage, ElMessageBox } from 'element-plus'
import { useNoticeStore } from '@/stores/notice.js'
import AppLayout from '@/components/layout/AppLayout.vue'
import PageHeader from '@/components/common/PageHeader.vue'
import NoticeFormDialog from '@/components/admin/NoticeFormDialog.vue'

const noticeStore = useNoticeStore()

// 搜索表单
const searchForm = reactive({
  title: '',
  status: ''
})

// 分页
const pagination = reactive({
  pageNo: 1,
  pageSize: 10
})

// 对话框
const dialogVisible = ref(false)
const currentNotice = ref(null)

// 计算属性
const noticeList = computed(() => noticeStore.noticeList)
const total = computed(() => noticeStore.total)
const loading = computed(() => noticeStore.loading)

// 方法
const getStatusType = (status) => {
  const types = {
    DRAFT: 'info',
    PUBLISHED: 'success',
    REMOVED: 'danger'
  }
  return types[status] || 'info'
}

const loadData = async () => {
  const params = {
    ...searchForm,
    ...pagination
  }
  await noticeStore.fetchNoticeList(params)
}

const handleSearch = () => {
  pagination.pageNo = 1
  loadData()
}

const handleReset = () => {
  searchForm.title = ''
  searchForm.status = ''
  handleSearch()
}

const handleAdd = () => {
  currentNotice.value = null
  dialogVisible.value = true
}

const handleEdit = (row) => {
  currentNotice.value = { ...row }
  dialogVisible.value = true
}

const handlePublish = async (row) => {
  try {
    await ElMessageBox.confirm('确定要发布该公告吗？')
    const success = await noticeStore.updateNoticeStatus(row.id, 'PUBLISHED')
    if (success) {
      ElMessage.success('发布成功')
      loadData()
    }
  } catch {}
}

const handleUnpublish = async (row) => {
  try {
    await ElMessageBox.confirm('确定要下架该公告吗？')
    const success = await noticeStore.updateNoticeStatus(row.id, 'DRAFT')
    if (success) {
      ElMessage.success('下架成功')
      loadData()
    }
  } catch {}
}

const handleDelete = async (row) => {
  try {
    await ElMessageBox.confirm('确定要删除该公告吗？')
    const success = await noticeStore.deleteNotice(row.id)
    if (success) {
      ElMessage.success('删除成功')
      loadData()
    }
  } catch {}
}

const handleSuccess = () => {
  dialogVisible.value = false
  loadData()
}

const handlePageChange = (page) => {
  pagination.pageNo = page
  loadData()
}

const handleSizeChange = (size) => {
  pagination.pageSize = size
  pagination.pageNo = 1
  loadData()
}

onMounted(() => {
  loadData()
})
</script>

<style scoped>
.search-card {
  margin-bottom: var(--spacing-lg);
}
</style>
```

#### 4.3.2 公告表单对话框组件

**文件路径**: `src/components/admin/NoticeFormDialog.vue`

```vue
<template>
  <el-dialog
    v-model="visible"
    :title="isEdit ? '编辑公告' : '新增公告'"
    width="600px"
    @close="handleClose"
  >
    <el-form
      ref="formRef"
      :model="form"
      :rules="rules"
      label-width="80px"
    >
      <el-form-item label="公告标题" prop="title">
        <el-input
          v-model="form.title"
          placeholder="请输入公告标题"
          maxlength="128"
          show-word-limit
        />
      </el-form-item>
      
      <el-form-item label="公告内容" prop="content">
        <el-input
          v-model="form.content"
          type="textarea"
          :rows="8"
          placeholder="请输入公告内容"
          maxlength="2000"
          show-word-limit
        />
      </el-form-item>
      
      <el-form-item label="状态" prop="status">
        <el-radio-group v-model="form.status">
          <el-radio value="DRAFT">草稿</el-radio>
          <el-radio value="PUBLISHED">发布</el-radio>
        </el-radio-group>
      </el-form-item>
    </el-form>
    
    <template #footer>
      <el-button @click="handleClose">取消</el-button>
      <el-button type="primary" @click="handleSubmit" :loading="loading">
        确定
      </el-button>
    </template>
  </el-dialog>
</template>

<script setup>
import { ref, computed, watch } from 'vue'
import { ElMessage } from 'element-plus'
import { useNoticeStore } from '@/stores/notice.js'

const props = defineProps({
  modelValue: Boolean,
  notice: Object
})

const emit = defineEmits(['update:modelValue', 'success'])

const noticeStore = useNoticeStore()
const formRef = ref()

const visible = computed({
  get: () => props.modelValue,
  set: (val) => emit('update:modelValue', val)
})

const isEdit = computed(() => !!props.notice)

const form = ref({
  title: '',
  content: '',
  status: 'DRAFT'
})

const rules = {
  title: [
    { required: true, message: '请输入公告标题', trigger: 'blur' },
    { min: 2, max: 128, message: '长度在 2 到 128 个字符', trigger: 'blur' }
  ],
  content: [
    { required: true, message: '请输入公告内容', trigger: 'blur' },
    { min: 10, max: 2000, message: '长度在 10 到 2000 个字符', trigger: 'blur' }
  ],
  status: [
    { required: true, message: '请选择状态', trigger: 'change' }
  ]
}

const loading = ref(false)

watch(() => props.notice, (newVal) => {
  if (newVal) {
    form.value = { ...newVal }
  } else {
    form.value = {
      title: '',
      content: '',
      status: 'DRAFT'
    }
  }
}, { immediate: true })

const handleClose = () => {
  visible.value = false
  formRef.value?.resetFields()
}

const handleSubmit = async () => {
  try {
    await formRef.value.validate()
    loading.value = true
    
    let success = false
    if (isEdit.value) {
      success = await noticeStore.updateNotice(props.notice.id, form.value)
    } else {
      success = await noticeStore.createNotice(form.value)
    }
    
    if (success) {
      ElMessage.success(isEdit.value ? '更新成功' : '创建成功')
      emit('success')
      handleClose()
    }
  } finally {
    loading.value = false
  }
}
</script>
```

---

## 5. 路由配置

### 5.1 管理端路由

**文件路径**: `src/router/admin.js`

```javascript
{
  path: '/admin/notices',
  name: 'AdminNotices',
  component: () => import('@/views/admin/Notices.vue'),
  meta: {
    title: '公告管理',
    requiresAuth: true,
    permission: 'admin:notice:view'
  }
}
```

### 5.2 用户端路由

**文件路径**: `src/router/user.js`

```javascript
{
  path: '/notices',
  name: 'UserNotices',
  component: () => import('@/views/user/NoticeList.vue'),
  meta: {
    title: '系统公告',
    requiresAuth: false
  }
}
```

---

## 6. 开发任务清单

### 6.1 后端开发任务
- [ ] 创建公告实体类
- [ ] 创建公告Mapper接口
- [ ] 创建公告Service接口及实现
- [ ] 创建公告Controller
- [ ] 添加权限注解配置
- [ ] 单元测试编写

### 6.2 前端开发任务
- [ ] 创建API接口文件
- [ ] 创建状态管理Store
- [ ] 实现管理端公告列表页面
- [ ] 实现公告表单对话框组件
- [ ] 实现用户端公告列表页面
- [ ] 添加路由配置
- [ ] 权限控制配置
- [ ] 页面样式优化

### 6.3 测试任务
- [ ] 接口测试
- [ ] 页面功能测试
- [ ] 权限测试
- [ ] 性能测试

---

## 7. 注意事项

1. **权限控制**: 管理端接口需要管理员权限
2. **数据安全**: 删除操作为软删除，数据保留
3. **缓存策略**: 用户端公告列表可缓存5分钟
4. **富文本**: 公告内容支持富文本编辑（后续扩展）
5. **通知推送**: 新公告发布时可考虑推送通知（后续扩展）
6. **国际化**: 支持多语言公告（后续扩展）

---

## 8. 错误码说明

| 错误码 | 说明 | 处理建议 |
|--------|------|----------|
| 10000 | 操作成功 | - |
| 40001 | 参数错误 | 检查请求参数 |
| 40004 | 公告不存在 | 检查公告ID |
| 40005 | 公告已删除 | 无法操作已删除公告 |
| 30004 | 无权限操作 | 检查用户权限 |
| 50000 | 系统异常 | 稍后重试 |

---

**文档版本**: v1.0  
**创建时间**: 2024-02-16  
**更新记录**: 初始版本创建